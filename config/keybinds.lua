-- Key bindings are evaluated by the modal input system in P5.
keybinds = {
  { key = "Super+Enter", action = "spawn", command = { "ghostty" } },
  { key = "Super+Space", action = "switch_mode", mode = "layout_select" },
}
